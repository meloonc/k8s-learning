# docker pull secret
apiVersion: v1
kind: Secret
metadata:
  name: docker-registry-secret
  namespace: default
type: kubernetes.io/dockerconfigjson
data:
  # docker config json base64 编码 
  .dockerconfigjson: GVzdA==